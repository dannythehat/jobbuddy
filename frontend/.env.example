# Frontend Environment Variables for GCP Deployment
# These are build-time variables embedded in the React build

# API Configuration
REACT_APP_API_URL=https://jobbuddy-backend-us-central1.a.run.app

# Environment
REACT_APP_ENV=production
NODE_ENV=production

# Feature Flags
REACT_APP_ENABLE_ANALYTICS=true
REACT_APP_ENABLE_DEBUG=false

# Google OAuth (Optional)
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Application Info
REACT_APP_VERSION=1.0.0
REACT_APP_NAME=JobBuddy

# NOTES FOR GCP DEPLOYMENT:
# 1. These variables are set at BUILD TIME
# 2. Update REACT_APP_API_URL to match your backend Cloud Run URL
# 3. Variables are embedded in the JavaScript bundle
# 4. Set in cloudbuild.yaml or as Dockerfile build arguments
